<html>
    <head>
        <title></title>
        <script type="text/javascript" src="public/js/scripts.js"></script>
    </head>

    <body>
        <h1>Welcome {{username}}</h1>
        <form action="/home" method="POST">
            <input type="text" id="items" name="username" value="{{username}}" style="display: none;">
            <label>Name Of Your Todo List</label>
            <input type="text" id="items" name="todo_list_name" value="{{todoListName}}">
            <button type="submit" onClick="insertItems()">Send</button>
        </form>

        <div id="showResults">
            {{#each result}}
                <input type="text" id="items" name="username" value="{{this.username}}" style="display: none;">
                <h4 id="{{this.todo_list_name}}">{{this.todo_list_name}}</h4>
                <a href="/add?username={{this.username}}&todolistName={{this.todo_list_name}}">Add</a>
                <a href="/deleteList?username={{this.username}}&todolistName={{this.todo_list_name}}">Delete List</a>
            {{/each}}
        </div>
    </body>
</html>